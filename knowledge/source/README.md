# knowledge/source/

LLM-distilled pages, one per raw artifact, in git. Written by `/wiki-ingest`;
mirrors `../raw/` provenance subdirs. Naming: `<year>-<firstAuthor>-<slug>.md`.

Regen-able — don't hand-edit. Fix the distillation template or the raw
artifact, then re-ingest. Synthesis across sources belongs in `../wiki/`.
